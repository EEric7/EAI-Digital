@using PortfolioDigital.Web.Models.Commun
<!-- Navigation-->
<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container px-4 px-lg-5">
        <a href="#" class="navbar-brand" >
            <img src="@Url.Content(Model.UserModel.EnseigneIcon)" alt="EAI Digital" width="60" height="60">
        </a>
        <a href="#" class="navbar-brand">
            @Model.UserModel.UserName
        </a>
        
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ms-auto">
                @foreach(var item in UIText.MenuItems)
                {
                    <li class="nav-item">
                        <a class="nav-link" href="@item.Path">@item.Name</a>
                    </li>
                }
                 <li class="nav-item">
                        <a class="nav-link" href="@Url.Content(Model.UserModel.CVPath)" download>C.V</a>
                </li>
            </ul>
        </div>
    </div>
</nav>